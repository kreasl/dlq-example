{
  "name": "dlq-example",
  "version": "1.0.0",
  "description": "Serverless project with API Gateway, Lambda, SQS, CloudWatch, and Step Functions",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "serverless offline",
    "deploy": "serverless deploy",
    "deploy:prod": "serverless deploy --stage prod",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "aws-sdk": "^2.1450.0",
    "serverless": "^3.38.0"
  },
  "devDependencies": {
    "serverless-offline": "^13.3.0"
  }
}
